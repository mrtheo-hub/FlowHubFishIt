-- [[ FLOW HUB | FISHING SYSTEM ]]
-- Version: Alpha 1.0.1
-- Owner: Dapz | Developer: Theo & Mon

-- [ 1. SAFE EXECUTOR CHECK ]
local execName = "Unknown"
pcall(function() 
    if type(identifyexecutor) == "function" then execName = identifyexecutor() end 
end)

-- [ 2. SAFE UI LOADER (ANTI NIL VALUE CRASH) ]
local success, ui_script = pcall(function()
    return game:HttpGet('https://raw.githubusercontent.com/shlexsoftware/Orion/main/source')
end)

if not success or not ui_script then
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "FLOW HUB ERROR",
        Text = "Gagal konek ke GitHub. Cek sinyal atau re-execute bro.",
        Duration = 7
    })
    return
end

local ui_func = loadstring(ui_script)
if type(ui_func) ~= "function" then
    game:GetService("StarterGui"):SetCore("SendNotification", {
        Title = "FLOW HUB ERROR",
        Text = "Ronix lu gagal nge-parse loadstring UI. Coba relog game.",
        Duration = 7
    })
    return -- Nge-stop eksekusi di sini biar ga crash "call a nil value"
end

local OrionLib = ui_func()

-- [ 3. SERVICES & CORE ]
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local HttpService = game:GetService("HttpService")
local RunService = game:GetService("RunService")
local TeleportService = game:GetService("TeleportService")
local Lighting = game:GetService("Lighting")
local ReplicatedStorage = game:GetService("ReplicatedStorage")

local requestFunc = (type(syn) == "table" and type(syn.request) == "function" and syn.request) or
                    (type(http) == "table" and type(http.request) == "function" and http.request) or
                    (type(http_request) == "function" and http_request) or
                    (type(request) == "function" and request)

getgenv().FlowConfig = {
    WebhookURL = "",
    Filters = {Common=false, Uncommon=false, Rare=false, Epic=false, Legendary=true, Mythic=true, Secret=true, Forgotten=true},
    Farming = false, Delay = 3.0, AutoSell = false, WalkWater = false, AntiDrown = false
}

-- [ UI INITIALIZATION ]
local Window = OrionLib:MakeWindow({
    Name = "Flow Hub [Alpha 1.0.1] | Exec: " .. execName, 
    HidePremium = false, 
    SaveConfig = true, 
    ConfigFolder = "FlowHubAlpha"
})

-- [[ TABS ]]
local MainTab = Window:MakeTab({Name = "Main", Icon = "rbxassetid://4483345998", PremiumOnly = false})
local InfoTab = Window:MakeTab({Name = "Info", Icon = "rbxassetid://4483345998", PremiumOnly = false})
local PlayerTab = Window:MakeTab({Name = "Player", Icon = "rbxassetid://4483345998", PremiumOnly = false})
local FishingTab = Window:MakeTab({Name = "Fishing", Icon = "rbxassetid://4483345998", PremiumOnly = false})
local TeleportTab = Window:MakeTab({Name = "Teleport", Icon = "rbxassetid://4483345998", PremiumOnly = false})
local WebhookTab = Window:MakeTab({Name = "Webhook", Icon = "rbxassetid://4483345998", PremiumOnly = false})
local UtilsTab = Window:MakeTab({Name = "Utilities", Icon = "rbxassetid://4483345998", PremiumOnly = false})
local PerfTab = Window:MakeTab({Name = "Performance", Icon = "rbxassetid://4483345998", PremiumOnly = false})

-- [[ MAIN TAB ]]
MainTab:AddLabel("© 2026 Flow Hub - All Rights Reserved.")
MainTab:AddLabel("Owner: Dapz")
MainTab:AddLabel("Developer: Theo & Mon")
MainTab:AddButton({
    Name = "Join Our Discord",
    Callback = function()
        setclipboard("https://discord.gg/WsK2db3YHN")
        OrionLib:MakeNotification({Name = "Discord", Content = "Link copied to clipboard!", Time = 3})
    end
})

-- [[ INFO TAB ]]
InfoTab:AddLabel("=== CHANGELOG ===")
InfoTab:AddLabel("[+] Alpha 1.0.1 Update")
InfoTab:AddLabel("[+] Added Safe UI Loader for Android")
InfoTab:AddLabel("[+] Bypassed nil value crash on Ronix")
InfoTab:AddLabel("[+] Fixed webhook rarity delays")

-- [[ WEBHOOK SYSTEM ]]
local function sendWebhook(data)
    if FlowConfig.WebhookURL == "" or type(requestFunc) ~= "function" then return end
    task.spawn(function()
        pcall(function()
            requestFunc({
                Url = FlowConfig.WebhookURL,
                Method = "POST",
                Headers = {["Content-Type"] = "application/json"},
                Body = HttpService:JSONEncode(data)
            })
        end)
    end)
end

local function getActiveRarities()
    local active = {}
    for k, v in pairs(FlowConfig.Filters) do if v then table.insert(active, k) end end
    return #active > 0 and table.concat(active, ", ") or "None"
end

local function getServerPlayersData()
    local pData = ""
    for _, p in pairs(Players:GetPlayers()) do
        local total = (p:FindFirstChild("leaderstats") and p.leaderstats:FindFirstChild("Caught")) and p.leaderstats.Caught.Value or "Unknown"
        local rarest = (p:FindFirstChild("leaderstats") and p.leaderstats:FindFirstChild("Rarest")) and p.leaderstats.Rarest.Value or "Unknown"
        pData = pData .. "👤 **" .. p.Name .. "**\n> Total: `" .. total .. "` | Rarest: `" .. rarest .. "`\n"
    end
    if pData == "" then pData = "No players found." end
    return pData
end

Players.PlayerAdded:Connect(function(p)
    sendWebhook({
        ["embeds"] = {{
            ["title"] = "📥 Player Joined",
            ["color"] = 0x00FF00,
            ["fields"] = {
                {["name"] = "Player", ["value"] = "```"..p.Name.."```", ["inline"] = true},
                {["name"] = "Total Server Players", ["value"] = "
http://googleusercontent.com/immersive_entry_chip/0
http://googleusercontent.com/immersive_entry_chip/1

Coba *execute* ulang. Kalau misal muncul notif Roblox pop-up "FLOW HUB ERROR" di kanan bawah layar, berarti emang executor Ronix lagi di-blok internetnya ke arah GitHub. Udah bisa nge-*load* UInya sekarang?
